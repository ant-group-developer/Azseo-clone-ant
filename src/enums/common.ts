export enum LOCALE {
    EN = 'en',
    VI = 'vi',
}

export enum STATUS {
    ACTIVE = 'Active',
    BLOCKED = 'Blocked',
    REMOVED = 'Removed',
}

export enum SortOrder {
    ASC = 'asc',
    DESC = 'desc',
}
